"""Bundled MCP servers shipped with the agent.

Each module here is a standalone MCP server that can be launched over stdio and
loaded via ``market_research_agent.tools.mcp.load_mcp_tools``. They serve as both
working data-source extensions and reference implementations engineers can copy.
"""
